public class VIPClient extends PremiumClient {

    public VIPClient(int clientID, String clientFname, String clientLname) {
        super(clientID, clientFname, clientLname);
        double addInterest = 0.01;
        double fee = 0;
    }

}
