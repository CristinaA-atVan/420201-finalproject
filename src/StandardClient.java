abstract class StandardClient extends Client {

    public StandardClient(int clientID, String clientFname, String clientLname) {
        super(clientID, clientFname, clientLname);
    }
    
}
