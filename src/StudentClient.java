public class StudentClient extends StandardClient {

    public StudentClient(int clientID, String clientFname, String clientLname) {
        super(clientID, clientFname, clientLname);
        double addInterest = 0;
        double fee = 0;
    }

}
